{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/registration.css') }}">

{% endblock %}
{% block body %}
    <div class="wrapper">

    {{ form_start(form, {method: 'GET', action: url('q') }) }}
    {{ form_widget(form) }}
    <button class="button_send" type="submit"> Zapisz</button>
    </div>
{% endblock %}

